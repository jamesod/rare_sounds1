<p id="notice"><%= notice %></p>

<h2>Your Cart</h2>
<ul>
  <% for item in @cart.line_items %>

      <li><strong>Release: </strong><%= item.music.release %> &times; <%= item.quantity  %>
      <br /><strong>Artist: </strong><%= item.music.artist %>
      <br /><strong>Price: </strong>€<%= item.music.price %></li>
      <br />

  <% end %>
  Total Price: <%= number_to_currency( @cart.total_price, :unit => '&euro;') %><br/>
 <br/>
  <%= button_to 'Empty cart', @cart, :method => :delete, :confirm => 'Are you sure?' %>
</ul>

<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
